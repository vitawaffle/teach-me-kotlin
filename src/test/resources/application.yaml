spring:
    profiles:
        active:
            - test
    jpa:
        hibernate:
            ddl-auto: none
    datasource:
        url: 'jdbc:postgresql://localhost:5432/server_test'
        username:
        password:
    liquibase:
        change-log: 'classpath:db/changelog/db.changelog-main.yaml'
        contexts: default, test
logging:
    level:
        root: INFO
    file: log/application-test.log
